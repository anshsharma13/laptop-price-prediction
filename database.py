"""
database.py — History saving stub.
MongoDB dependency removed so the app runs without a running MongoDB instance.
Silently skips saving; extend this to a real DB if needed later.
"""


def save_recommendation_history(search_params, recommended_models):
    """No-op stub. Silently skip saving to avoid MongoDB crashes."""
    pass
